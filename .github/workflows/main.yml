name: Keep App Awake
on:
  schedule:
    - cron: '0 */4 * * *' # Pings your app automatically every 4 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send keep-alive ping
        run: curl -s https://ai4all.streamlit.app > /dev/null
